'use strict';
// Crie uma função `showMessage()` que exiba `"Olá, mundo!"` no console.

//- Chame a função duas vezes.

function showMessage() {
    console.log('Olá, mundo!');
}

showMessage();
showMessage();
showMessage;