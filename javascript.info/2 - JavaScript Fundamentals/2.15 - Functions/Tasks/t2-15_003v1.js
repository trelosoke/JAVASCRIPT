'use strict';
//Write a function min(a,b) which returns the least of two numbers a and b.
//Versão com if e else

function min(a, b){
    if(a < b){
        return a;
    } else {
        return b;
    }
}