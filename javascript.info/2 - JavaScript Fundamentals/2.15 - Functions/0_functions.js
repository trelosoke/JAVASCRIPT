'use strict';
//Funções
    //Muito frequentemente precisamos performar uma ação semelhante em muitos locais do script.
    //Por exemplo, precisamos mostrar uma mensagem bonita quando um visitante dá "log in", "log out" e talvez em algum outro local.
    //Funções são as principais "building blocks" do programa. Como uma peça de lego, elas podem se encaixar em inúmeros lugares do código. Permitem que o código seja chamado várias vezes sem repetição.
    //Também servem para organizar o código, facilitam a manutenção e na divisão de tarefas. Tudo isso será abordado neste artigo.

    //Já vimos exemplos de funções  "build in (nativas)", como "alert(mensagem)", "prompt(mensagem, podrão)" e "confirm(pergunta)".