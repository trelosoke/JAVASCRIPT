'use strict';
//Crie uma função soma(a, b) que retorna a soma de dois números.

function soma(a, b) {
    return a + b;
}

function exibirSoma(a, b) {
    console.log(soma(a, b));
}

exibirSoma(10, 20);