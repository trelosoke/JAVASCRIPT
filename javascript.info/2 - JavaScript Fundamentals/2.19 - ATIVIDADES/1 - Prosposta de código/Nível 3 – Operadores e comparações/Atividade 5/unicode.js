'use strict';
//Compare "Zebra" < "apple" e explique o resultado.

console.log("Zebra" < "apple");

//O unicode é um padrão de códificação de caracteres, usando números para representar vários caracteres em computadores (como letras, a, b, c) de qualquer sistema de escrita existente, de forma consistente.
//A ordem considerada na comparação de strings é o número do unicode de cada caractere.
//No caso acima, as letras maiúsculas têm um número maior que as maiúsculas. Logo, já na primeira comparação entre 'Z' e 'a', o resultado se torna true..