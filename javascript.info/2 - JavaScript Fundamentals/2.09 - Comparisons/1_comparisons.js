'use strict';
//No JavaScript, comparações podem ser da seguinte forma:
    // 1. Maior/menor que: a > b, a < b;
    // 2. Maior/menor ou igual a: a >= b, a <= b;
    // 3. Igualdade: a == b (note que a = b seria uma atribuição);
    // 4. Diferente: a != b (na matemática comum, seria ≠)

    //Nesse capítulo, será aprofundado como elas funcionam no JS.