'use strict';
//What is the output of the script?

let nome = "Ilya";

console.log( `hello ${1}` ); // ? hello 1

console.log( `hello ${"name"}` ); // ? hello name

console.log( `hello ${nome}` ); // ? hello Ilya