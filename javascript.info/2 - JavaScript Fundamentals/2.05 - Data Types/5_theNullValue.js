'use strict';
//O valor "null"
    //Tipo exclusivo que só contém o valor "null".
    let age = null;

    //No JavaScript, null não é uma referencia a um objeto não existente ou um ponteiro nulo, como em outras linguagens.
    //Ele serve simplesmente para representar "nada", "vazio" ou "valor desconhecido".
    //O código acima declara que "age" é desconhecido. 