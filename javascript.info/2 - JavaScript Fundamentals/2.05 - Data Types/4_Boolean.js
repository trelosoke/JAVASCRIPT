'use strict'
//Boolean (tipo lógico)
    //O tipo booleano (boolean) contém somente dois valores: true e false.
    //É usualmente utilizada para armazenar valores de "sim/não", sendo true, sim, e false, não.
    //Por exemplo:
    let nameFieldChecked = true; //O campo de nome está marcado.
    let ageFirldChecked = false; //O campo de idade não está marcado.

    //Eles também vêm de resultados de comparações:
    let isGreater = 4 > 1;
    console.log(isGreater); //true, pois 4 é mais que 1.

    //Esse tipo será abordado posteriormente em "Logical Operators"