'use strict';
//Números
    //No JavaScript moderno, há dois tipos de números:
        // 1. Números regulares no JS são armazenados no formato IEEE-754 64-bit, também conhecido como "double precision floating point numbers". Esses são os números que estamos usando na maioria do tempo, e falaremos sobre eles nesse capítulo.

        // 2. Números BigInt representam inteiros de qualquer tamanho. Eles são necessários algumas vezes porque um número regular inteiro não pode ultrapassar (2^53 - 1) ou menos que -(2^53 - 1), como mencionamos antes do capítulo Data Types (2.05). Como 'bigints' são utilizados em poucas áreas, voltaremos a eles no capítulo BigInt.

    //Então aqui falaremos sobre números regulares. Vamos expandir nosso conhecimento sobre eles.