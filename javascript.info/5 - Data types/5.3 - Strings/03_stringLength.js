'use strict';
//Tamanho da string
    //A propriedade 'length' tem o tamanho da string:
        console.log('Meu\n'.length); //4 - M, e, u, \n

    //Note que \n é apenas um único caractere "especial", então o tamanho é realmente 4.

                //'length' é uma propriedade
                    //Pessoas que tiveram contato com algumas outras linguagens algumas vezes erram ao chamar 'str.length()' ao invés de só 'str.length'. Isso não funciona.

                    //Por favor note que 'str.length' é uma propriedade numérica, não uma função/método. Não há necessidade de adicionar os parênteses após. Não '.length()', mas '.length'.