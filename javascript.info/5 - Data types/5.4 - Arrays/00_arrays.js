'use strict';
//Arrays
    //Objetos permitem que você armazene coleções de valores indexados (índice) por chaves. Até aí tudo bem.

    //Mas frequentemente necessitamos de uma "coleção ordenada", na qual temos um 1º, 2º, 3º elemento e por aí vai. Por exemplo, precisamos disso para guardar uma lista de algo: usuários, mercadorias, elementos HTML, etc.

    //Não nos é conveniente usar um objeto nesse caso, pois ele não oferece métodos para gerenciar a ordem dos elementos. Não podemos inserir uma nova propriedade "entre" as já existente. Objetos só não foram feitos para isso.

    //Há uma estrutura de dados especial chamada 'Array', para armazenar coleções ordenadas.